[project]
name = "myapp-cli"
version = "1.0.0"
dependencies = [
    "typer>=0.9.0",
]

[tool.poetry]
name = "myapp-cli"
version = "1.0.0"

[tool.poetry.dependencies]
python = "^3.11"
typer = "^0.9.0"
myapp-shared = { path = "../shared" }
myapp-api = { path = "../api" }
